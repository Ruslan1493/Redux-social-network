import Button from '@mui/material/Button';

export const Mui = (props) => {

    return (
        <div>
            <div>
                <Button variant="contained">Content</Button>
            </div>
        </div>
    )   
}